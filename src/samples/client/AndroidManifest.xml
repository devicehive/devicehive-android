<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devicehive.android.client.sample"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".SampleClientApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AuthActivity"
            android:configChanges="orientation" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".NetworksActivity"
            android:label="@string/title_activity_networks"
            android:configChanges="orientation" >
        </activity>
        
        <activity
            android:name=".NetworkDevicesActivity"
            android:configChanges="orientation" >
        </activity>
        
         <activity
            android:name=".DeviceActivity"
            android:configChanges="orientation" >
        </activity>
        
         <activity
            android:name=".DeviceNotificationsActivity"
            android:configChanges="orientation" >
        </activity>
        
         <activity
            android:name=".DeviceSendCommandActivity"
            android:configChanges="orientation" >
        </activity>

        <service 
          android:name="com.dataart.android.devicehive.network.DeviceHiveApiService"
          android:exported="false"/>
    </application>

</manifest>